{
  "$schema": "https://schema.tauri.app/config/2",
  "$comment": "CI-only overlay merged over tauri.conf.json via `tauri build --config`. Turns ON signed updater artifacts (installer + .sig + latest.json). Kept OUT of the base config so local `pnpm desktop:build` stays keyless. Signing requires TAURI_SIGNING_PRIVATE_KEY(+_PASSWORD) in the environment. See .github/workflows/release-partnera-internal.yml.",
  "bundle": {
    "createUpdaterArtifacts": true
  }
}
