# Rust / Cargo build output
src-tauri/target/

# Tauri generated (schemas, etc.)
src-tauri/gen/

# Staged runtime resources — regenerated by `pnpm prepare:runtime`
# (server.cjs is an esbuild output; node.exe is the copied Node runtime, ~90 MB)
src-tauri/resources/

# Icon source scratch (icons/ is committed; this is the raw export)
app-icon.png
