{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Partnera Business desktop thin client. Deny-by-default: the loaded web content (the Business consoles served by the central API over HTTPS) uses NO Tauri IPC. External-link opening and navigation control are handled in Rust, so no filesystem, shell, http, process, or opener permission is exposed to web content. Only core window defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
