/**
 * @partnera/tracking-engine — capture touches and attribute conversions.
 *
 * Platform-agnostic touch/order/refund records plus a configurable attribution
 * resolver. Cookieless-friendly (coupon attribution is first-class). Future
 * server-side / offline / API tracking slot in behind the same claim model.
 * See docs/05-tracking-engine.md.
 */
export * from "./events";
export * from "./attribution";
