/**
 * @partnera/analytics — KPIs, metric events, reports, funnels, dashboards.
 *
 * A small, pure aggregation core over a normalized event stream. Heavier BI is a
 * future concern that consumes the same events. See docs/02-architecture.md.
 */
export * from "./metrics";
export * from "./funnel";
